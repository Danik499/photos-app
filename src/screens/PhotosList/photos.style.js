import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
    titleText: {
        textAlign: "center",
        fontSize: 20,
        paddingBottom: 20
    }
})

export default styles